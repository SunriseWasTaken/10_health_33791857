<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('partials/header') %>
</head>
<body>
  <%- include('partials/sidebar') %>

  <main class="main-content">
    <div class="dashboard-header">
      <h1>Welcome Back</h1>
      <p class="subtitle">Sign in to keep tracking your health and fitness journey.</p>
    </div>

    <div class="dashboard-grid">
      <div class="card card-large">
        <h2>Login</h2>

        <% if (error) { %>
          <div
            style="
              padding: 12px 16px;
              border-radius: 10px;
              background: rgba(255, 122, 92, 0.15);
              border: 1px solid rgba(255, 122, 92, 0.4);
              color: #ffb88c;
              margin-bottom: 12px;
              font-weight: 600;
            ">
            <%= error %>
          </div>
        <% } %>

        <form method="POST" action="/login">
          <label for="username">Username</label>
          <input
            id="username"
            name="username"
            type="text"
            value="<%= (form && form.username) ? form.username : '' %>"
            placeholder="gold"
            required
            autocomplete="username"
          />

          <label for="password">Password</label>
          <input
            id="password"
            name="password"
            type="password"
            placeholder="smiths123ABC$"
            required
            autocomplete="current-password"
          />

          <button type="submit" class="btn">Sign In</button>
        </form>

        <p style="margin-top: 16px; color: var(--muted);">
          Need an account?
          <a href="/signup" style="color: var(--peach); font-weight: 600;">Create one now</a>.
        </p>
      </div>

      <div class="card">
        <h2>PulseLog Highlights</h2>
        <p>Logging in unlocks:</p>
        <ul style="padding-left: 18px; line-height: 1.7;">
          <li>Workout history &amp; trends</li>
          <li>Personalized weather nudges</li>
          <li>Quick links to log new sessions</li>
        </ul>
        <p style="color: var(--muted); margin-top: auto;">
          Use username <strong>gold</strong> with the provided coursework password.
        </p>
      </div>
    </div>
  </main>

  <%- include('partials/footer') %>
</body>
</html>
